/** S3 key for the SQLite DB file */
export const SEARCH_INDEX_DB_S3_KEY = 'search-index/flexsearch_index.db';

/** Local path for SQLite DB in Lambda environment */
export const LOCAL_DB_PATH = '/tmp/flexsearch_index.db';
