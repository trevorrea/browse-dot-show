{
  "name": "listen-fair-play",
  "version": "1.0.0",
  "private": true,
  "description": "A system for processing and analyzing podcast content",
  "scripts": {
    "dev:client": "pnpm --filter listen-fair-play dev",
    "dev:rss": "pnpm --filter podcast-processing run dev:rss",
    "dev:whisper": "pnpm --filter podcast-processing run dev:whisper",
    "build:processing": "pnpm --filter podcast-processing build",
    "build:client": "pnpm --filter listen-fair-play build",
    "install:all": "pnpm install",
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=8.0.0"
  }
} 