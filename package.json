{
  "name": "listen-fair-play",
  "version": "1.0.0",
  "private": true,
  "description": "A system for processing and analyzing podcast content",
  "scripts": {
    "preinstall": "node scripts/check-pnpm.js",
    "dev:processing": "pnpm --filter podcast-processing dev",
    "build:processing": "pnpm --filter podcast-processing build",
    "install:all": "pnpm install",
    "setup": "bash scripts/setup.sh"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=8.0.0"
  }
} 