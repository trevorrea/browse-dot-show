{
  "extends": "../../../tsconfig.base.lambdas.json",
  "compilerOptions": {
    // We put output files here temporarily, 
    // but move them into `./aws-dist` (& cleanup `./temp-dist`) 
    //via `/scripts/pnpm-deploy-with-versions-fix.sh`
    "outDir": "./temp-dist",
    "rootDir": "./",
  },
} 